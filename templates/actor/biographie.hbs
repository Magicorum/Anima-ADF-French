{{! biographie Tab }}
<section
  class='tab biographie {{tab.cssClass}}'
  data-group='primary'
  data-tab='biographie'
>
  {{! Editors must receive enriched text data from getData to properly handle rolls, UUID links, etc. }}
  {{#if editable}} 
    <prose-mirror name="system.biographie" data-document-u-u-i-d="{{actor.uuid}}" value="{{system.biographie}}" collaborate="true" toggled="true">
      {{{enrichedBiography}}}
    </prose-mirror>
  {{else}} {{{enrichedBiography}}}
  {{/if}}
</section>